namespace Reportr.Components.Metrics
{
    /// <summary>
    /// Defines the different chart value types that are supported
    /// </summary>
    public enum ChartValueType
    {
        Custom = 0,
        DateTime = 1,
        Double = 2
    }
}
